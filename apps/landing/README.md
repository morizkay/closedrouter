# ClosedRouter landing

Astro marketing site. Deploy on Vercel with **Root Directory** `apps/landing`.

```bash
cd apps/landing
npm install
npm run dev      # http://localhost:4321
npm run check    # astro check
npm run build    # static + @astrojs/vercel output
```

The Rust gateway is not deployed here. See the repository root README.
